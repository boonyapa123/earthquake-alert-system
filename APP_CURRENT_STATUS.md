# 📊 สถานะปัจจุบันของแอพ eQNode

## ✅ สิ่งที่ทำงานถูกต้องแล้ว

### 1. 🎯 การแยกกลุ่มตามประเภทเซ็นเซอร์ ✅

**หน้า MQTT Real-time แสดง:**
- ✅ แยกเป็น 3 กลุ่มหลัก:
  - 🌍 เซ็นเซอร์แผ่นดินไหว (สีแดง)
  - 🌊 เซ็นเซอร์คลื่นซึนามิ (สีน้ำเงิน)
  - 📐 เซ็นเซอร์วัดความเอียง (สีม่วง)

- ✅ แต่ละกลุ่มแยกตามความรุนแรง:
  - 🚨 รุนแรงมาก (Critical)
  - ⚠️ รุนแรง (High)
  - 🟢 ปกติ (Normal)

**ตัวอย่างจากภาพ:**
```
🌍 เซ็นเซอร์แผ่นดินไหว [32 รายการ] →
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 รุนแรงมาก (32)
  [EQNODE] EQC-28562faa0b60 - CRITICAL
  Magnitude: 10.00 Richter
  Lat: 13.9032, Lon: 100.5330
```

### 2. 🔔 ค่า Threshold การแจ้งเตือน ✅

**ค่าที่ใช้ตอนนี้ (ตามมาตรฐานสากล):**

| เซ็นเซอร์ | ค่าขั้นต่ำแจ้งเตือน | Critical | High | Moderate |
|-----------|---------------------|----------|------|----------|
| 🌍 แผ่นดินไหว | >= 4.0 Richter | >= 7.0 | 6.0-6.9 | 5.0-5.9 |
| 🌊 คลื่นซึนามิ | >= 0.5 เมตร | >= 3.0 | 1.0-2.9 | 0.5-0.9 |
| 📐 ความเอียงตึก | >= 0.5 องศา | >= 2.0 | 1.0-1.9 | 0.5-0.9 |

**โค้ดใน `mqtt_manager.dart`:**
```dart
bool _shouldAlert(MqttLog log) {
  switch (log.sensorType) {
    case 'earthquake':
      return log.magnitude >= 4.0;  // ✅ ถูกต้อง
    
    case 'tsunami':
      return log.magnitude >= 0.5;  // ✅ ถูกต้อง
    
    case 'tilt':
      return log.magnitude >= 0.5;  // ✅ ถูกต้อง
    
    default:
      return log.magnitude >= 4.0;
  }
}
```

### 3. 🎨 การแสดงผลตามประเภท ✅

**แต่ละประเภทมี:**
- ✅ สีเฉพาะ (แดง/น้ำเงิน/ม่วง)
- ✅ ไอคอนเฉพาะ (📊/🌊/📐)
- ✅ หน่วยเฉพาะ (Richter/เมตร/องศา)
- ✅ Badge สีต่างกัน

**ตัวอย่างจากโค้ด:**
```dart
// Earthquake
unit = 'Richter';
magnitudeIcon = Icons.speed;
magnitudeLabel = 'Magnitude';

// Tsunami
unit = 'เมตร';
magnitudeIcon = Icons.height;
magnitudeLabel = 'ความสูงคลื่น';

// Tilt
unit = 'องศา';
magnitudeIcon = Icons.rotate_right;
magnitudeLabel = 'มุมเอียง';
```

### 4. 🔗 การคลิกเข้าดูรายละเอียด ✅

**Header แต่ละประเภทคลิกได้:**
```dart
InkWell(
  onTap: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => SensorDetailScreen(
          sensorType: sensorType,
          title: title,
          color: color,
          icon: icon,
          logs: logs,
        ),
      ),
    );
  },
  ...
)
```

---

## ⚠️ ปัญหาที่พบจากภาพ

### 1. ❌ ข้อมูลทดสอบไม่สมจริง

**ปัญหา:**
- Magnitude: **10.00 Richter** (สูงเกินจริง)
- แจ้งเตือนบ่อยเกินไป (32 รายการ CRITICAL)

**สาเหตุ:**
- ข้อมูลจาก simulator หรือ EQNODE ส่งค่าผิดปกติ
- อาจเป็นการทดสอบที่ส่งค่าสูงเกินจริง

**วิธีแก้:**
```javascript
// ใน backend/simulate-earthquake.js
// ปรับค่า magnitude ให้สมจริง
const magnitude = (Math.random() * 3 + 2).toFixed(1); // 2.0 - 5.0
```

### 2. ⚠️ การแจ้งเตือนถี่เกินไป

**ปัญหา:**
- แจ้งเตือนทุก 3 วินาที (cooldown)
- ถ้าข้อมูลมาบ่อย จะแจ้งเตือนบ่อยตาม

**วิธีแก้ (ถ้าต้องการลดการแจ้งเตือน):**

#### Option 1: เพิ่ม Cooldown
```dart
// ใน mqtt_manager.dart
final cooldown = const Duration(seconds: 30); // เพิ่มจาก 3 เป็น 30 วินาที
```

#### Option 2: แจ้งเตือนเฉพาะ Critical และ High
```dart
Future<void> _processLog(MqttLog newLog) async {
  // แจ้งเตือนเฉพาะ magnitude >= 5.0 (High ขึ้นไป)
  if (newLog.type == 'earthquake' && newLog.magnitude >= 5.0) {
    await _sendEarthquakeAlert(newLog);
  }
  ...
}
```

#### Option 3: แจ้งเตือนเฉพาะครั้งแรกของแต่ละ Device
```dart
final Set<String> _alertedDevices = {};

Future<void> _sendEarthquakeAlert(MqttLog log) async {
  // ตรวจสอบว่าเคยแจ้งเตือน device นี้แล้วหรือไม่
  if (_alertedDevices.contains(log.deviceId)) {
    return; // ข้าม
  }
  
  _alertedDevices.add(log.deviceId);
  
  // ส่ง notification
  ...
}
```

---

## 📋 สรุปการทำงานปัจจุบัน

### ✅ ทำงานถูกต้อง:
1. ✅ แยกกลุ่มตามประเภทเซ็นเซอร์ (3 ประเภท)
2. ✅ แยกความรุนแรงภายในแต่ละประเภท
3. ✅ ใช้สีและไอคอนเฉพาะแต่ละประเภท
4. ✅ แสดงหน่วยที่เหมาะสม (Richter/เมตร/องศา)
5. ✅ คลิก Header เพื่อดูรายละเอียดแต่ละประเภท
6. ✅ ค่า Threshold ตามมาตรฐานสากล

### ⚠️ ต้องปรับปรุง:
1. ⚠️ ข้อมูลทดสอบไม่สมจริง (10.00 Richter)
2. ⚠️ การแจ้งเตือนถี่เกินไป (ถ้าข้อมูลมาบ่อย)

---

## 🔧 คำแนะนำการแก้ไข

### 1. ปรับข้อมูลทดสอบให้สมจริง

**แก้ไข `backend/simulate-earthquake.js`:**
```javascript
// ปรับค่า magnitude ให้สมจริง
const magnitude = (Math.random() * 3 + 2).toFixed(1); // 2.0 - 5.0 Richter

// หรือสุ่มตามความน่าจะเป็นจริง
const magnitudes = [
  2.5, 2.8, 3.0, 3.2, 3.5, // 70% - ต่ำ
  4.0, 4.2, 4.5,           // 20% - ปานกลาง
  5.0, 5.5,                // 8% - สูง
  6.0, 6.5                 // 2% - สูงมาก
];
const magnitude = magnitudes[Math.floor(Math.random() * magnitudes.length)];
```

### 2. ลดการแจ้งเตือน (เลือก 1 วิธี)

#### วิธีที่ 1: เพิ่ม Cooldown (แนะนำ)
```dart
// ใน mqtt_manager.dart
final cooldown = const Duration(minutes: 5); // แจ้งเตือนไม่เกิน 1 ครั้งต่อ 5 นาที
```

#### วิธีที่ 2: แจ้งเตือนเฉพาะ High และ Critical
```dart
Future<void> _processLog(MqttLog newLog) async {
  // แจ้งเตือนเฉพาะ magnitude >= 5.0
  if (newLog.type == 'earthquake' && newLog.magnitude >= 5.0) {
    await _sendEarthquakeAlert(newLog);
  }
  ...
}
```

#### วิธีที่ 3: แจ้งเตือนครั้งแรกเท่านั้น
```dart
// เพิ่ม Set เก็บ device ที่แจ้งเตือนแล้ว
final Set<String> _alertedDevices = {};

Future<void> _sendEarthquakeAlert(MqttLog log) async {
  // ตรวจสอบว่าเคยแจ้งเตือนแล้วหรือไม่
  final key = '${log.deviceId}_${log.magnitude.toStringAsFixed(1)}';
  if (_alertedDevices.contains(key)) {
    return;
  }
  
  _alertedDevices.add(key);
  
  // ส่ง notification
  ...
}
```

---

## 🎯 สรุปสุดท้าย

### ตอบคำถาม: "ตอนนี้การทำงานของแอพเป็นตามที่บอกและต้องการไหม"

**คำตอบ: ✅ ใช่ แต่มีข้อแนะนำเพิ่มเติม**

#### ✅ ทำงานถูกต้องแล้ว:
1. ✅ หน้า MQTT Real-time แสดงการ์ดแยกตามประเภทเซ็นเซอร์
2. ✅ คลิกเข้าไปเห็น device และค่าต่างๆ
3. ✅ การแจ้งเตือนใช้ค่าสากล (4.0 Richter, 0.5 เมตร, 0.5 องศา)

#### ⚠️ ควรปรับปรุง:
1. ⚠️ **ข้อมูลทดสอบ**: ปรับให้สมจริง (2-6 Richter แทน 10.0)
2. ⚠️ **การแจ้งเตือน**: เพิ่ม cooldown หรือแจ้งเตือนเฉพาะ High/Critical

---

## 📝 ขั้นตอนถัดไป

### 1. ปรับข้อมูลทดสอบ (แนะนำ)
```bash
# แก้ไข backend/simulate-earthquake.js
# เปลี่ยน magnitude จาก 10.0 เป็น 2.0-6.0
```

### 2. ปรับการแจ้งเตือน (ถ้าต้องการ)
```bash
# แก้ไข lib/services/mqtt_manager.dart
# เพิ่ม cooldown หรือเงื่อนไขการแจ้งเตือน
```

### 3. ทดสอบ
```bash
flutter clean
flutter pub get
flutter run
```

---

**สถานะ**: ✅ แอพทำงานถูกต้องตามที่ออกแบบ แต่ควรปรับข้อมูลทดสอบให้สมจริง

**Last Updated**: November 21, 2025
