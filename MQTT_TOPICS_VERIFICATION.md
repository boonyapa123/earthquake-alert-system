# ✅ การตรวจสอบ Topics และข้อมูล MQTT

## 📊 Topics ที่มีใน MQTT Broker

จาก MQTT Explorer พบ topics ทั้งหมด:

| Topic | จำนวน Topics | จำนวน Messages | ประเภท | แอพ Subscribe? |
|-------|-------------|---------------|--------|---------------|
| **eqnode.tarita** | 725 | 15,783 | แผ่นดินไหว | ✅ ใช่ |
| **eqnode.cnx** | 415 | 436 | แผ่นดินไหว | ✅ ใช่ (เพิ่มแล้ว) |
| **pmac** | 1 | 4 | สถานะอุปกรณ์ | ✅ ใช่ |
| **TPO** | 1 | 6,322 | ไฟฟ้า | ✅ ใช่ |
| **earthquake** | 2 | 24 | Simulator | ✅ ใช่ |
| **SSYS** | 160 | 220,640 | ระบบ | ❌ ไม่ (ไม่จำเป็น) |
| **pems** | 1 | 92 | พลังงาน | ❌ ไม่ (ไม่จำเป็น) |
| **TANK** | - | - | ถังน้ำ | ❌ ไม่ (ไม่จำเป็น) |
| **FLOWMETER** | - | - | มิเตอร์น้ำ | ❌ ไม่ (ไม่จำเป็น) |
| **DT67-001001** | - | - | ไฟฟ้า | ❌ ไม่ (ไม่จำเป็น) |

---

## ✅ Topics ที่แอพ Subscribe

### 1. แผ่นดินไหว (Earthquake Monitoring)
```dart
'eqnode.tarita/hub/#'    // ข้อมูลแผ่นดินไหวหลัก (15,783 messages)
'eqnode.cnx/hub/#'       // ข้อมูลแผ่นดินไหว CNX (436 messages)
'earthquake/data'        // ข้อมูลจาก simulator (24 messages)
'earthquake/alert'       // การแจ้งเตือน
```

### 2. อุปกรณ์ตรวจวัด (Device Monitoring)
```dart
'pmac/#'                 // อุปกรณ์ PMAC (4 messages)
'TPO/#'                  // อุปกรณ์ TPO (6,322 messages)
```

---

## 📋 โครงสร้างข้อมูลที่รองรับ

### A. ข้อมูลจาก EQNODE (อุปกรณ์จริง)

**Topic Pattern**: `eqnode.tarita/hub/1/{device_id}/eqdata/null`

**ข้อมูลที่ส่งมา**:
```json
{
  "did": "EQC-28562faa0b60",
  "ts": "2025-11-21 00:31:27.716",
  "lat": 13.903011,
  "lon": 100.533103,
  "alt": 30,
  "ax": 0.001056,
  "ay": 0.014689,
  "az": -0.115818,
  "t1": 30.67,
  "rms": 0.1168,
  "pga": 0.1213,
  "fq": 2.1,
  "wid": 255,
  "wave": "Unknown"
}
```

**การแปลงข้อมูล**:
- `did` → `deviceId`
- `pga` → `magnitude` (คำนวณ: `pga * 1000`)
- `lat`, `lon` → `location` (แสดงพิกัด)
- `ts` → `timestamp`

**ผลลัพธ์ในแอพ**:
```
Device: EQC-28562faa0b60
Magnitude: 0.12 Richter
Location: Lat: 13.9030, Lon: 100.5331
Time: 00:31:27
```

---

### B. ข้อมูลจาก Simulator

**Topic**: `earthquake/data`

**ข้อมูลที่ส่งมา**:
```json
{
  "deviceId": "EQC-SIM-001",
  "timestamp": "2025-11-20T17:35:48.421Z",
  "magnitude": 5.3,
  "latitude": 16.4419,
  "longitude": 102.8360,
  "depth": 25,
  "location": "Khon Kaen",
  "intensity": "high"
}
```

**การแปลงข้อมูล**:
- ใช้ข้อมูลตรงๆ ไม่ต้องแปลง

**ผลลัพธ์ในแอพ**:
```
Device: EQC-SIM-001
Magnitude: 5.3 Richter
Location: Khon Kaen
Time: 17:35:48
```

---

### C. ข้อมูลสถานะอุปกรณ์

**Topic Pattern**: 
- `eqnode.tarita/hub/1/{device_id}/ping/{timestamp}`
- `pmac/{device_id}/status`

**การจัดการ**:
- ไม่แสดงในหน้า "MQTT Real-time"
- Log เท่านั้น (ถ้าเปิด debug mode)

---

## 🔧 การประมวลผลข้อมูล

### 1. กรองตาม Topic

```dart
if (topic.contains('/eqdata/')) {
  // ข้อมูลแผ่นดินไหวจาก EQNODE → แสดงในแอพ
  await _processEarthquakeData(topic, data);
}
else if (topic.contains('earthquake/data')) {
  // ข้อมูลจาก simulator → แสดงในแอพ
  await _processEarthquakeData(topic, data);
}
else if (topic.contains('/ping/') || topic.contains('/status')) {
  // สถานะอุปกรณ์ → log เท่านั้น
  print('Device status update');
}
```

### 2. แปลงข้อมูล

```dart
// Device ID
String deviceId = data['did'] ?? data['deviceId'] ?? 'UNKNOWN';

// Magnitude
double magnitude = 0.0;
if (data.containsKey('pga')) {
  // จาก EQNODE: แปลง PGA เป็น magnitude
  magnitude = (data['pga'] * 1000).clamp(0.1, 10.0);
} else if (data.containsKey('magnitude')) {
  // จาก simulator: ใช้ตรงๆ
  magnitude = data['magnitude'];
}

// Location
String location = 'Unknown Location';
if (data.containsKey('location')) {
  location = data['location'];
} else if (data.containsKey('lat') && data.containsKey('lon')) {
  location = 'Lat: ${lat.toStringAsFixed(4)}, Lon: ${lon.toStringAsFixed(4)}';
}

// Timestamp
DateTime timestamp = DateTime.now();
if (data.containsKey('ts')) {
  timestamp = DateTime.parse(data['ts'].replaceAll(' ', 'T'));
} else if (data.containsKey('timestamp')) {
  timestamp = DateTime.parse(data['timestamp']);
}
```

### 3. กรองข้อมูล

```dart
// แสดงเฉพาะข้อมูลที่มี magnitude > 0
if (magnitude > 0.0) {
  final log = MqttLog(
    deviceId: deviceId,
    magnitude: magnitude,
    timestamp: timestamp,
    location: location,
    type: 'earthquake',
    ownerId: 'system',
  );
  
  _processLog(log);
  
  // แจ้งเตือนสำหรับแผ่นดินไหวรุนแรง
  if (magnitude >= 3.0) {
    await _sendEarthquakeAlert(log);
  }
}
```

---

## ✅ การทดสอบ

### 1. ข้อมูลจาก EQNODE จริง

แอพควรแสดง:
- Device ID: `EQC-28562faa0b60`, `EQC-28562fa9d7a8`, etc.
- Magnitude: 0.01 - 1.0 Richter (จาก PGA)
- Location: พิกัด Lat/Lon
- Time: เวลาจริงจากอุปกรณ์

### 2. ข้อมูลจาก Simulator

แอพควรแสดง:
- Device ID: `EQC-SIM-001`, `EQC-SIM-002`, etc.
- Magnitude: 2.0 - 6.5 Richter
- Location: Bangkok, Chiang Mai, Phuket, Khon Kaen, Pattaya
- Time: เวลาที่ส่ง

### 3. ข้อมูลสถานะ (ไม่แสดง)

- EQNODE ping → log เท่านั้น
- PMAC status → log เท่านั้น
- TPO data → log เท่านั้น

---

## 📱 ผลลัพธ์ในแอพ

### หน้า "MQTT Real-time" ควรแสดง:

```
✅ MQTT Status: CONNECTED
   Total messages: 25

Real-time MQTT Data
━━━━━━━━━━━━━━━━━━━━━━━━━━
🌍 EQC-SIM-010
   Magnitude: 6.4 Richter
   Location: Pattaya
   Time: 17:36:15
   Status: HIGH
━━━━━━━━━━━━━━━━━━━━━━━━━━
🌍 EQC-28562faa0b60
   Magnitude: 0.12 Richter
   Location: Lat: 13.9030, Lon: 100.5331
   Time: 00:31:27
   Status: NORMAL
━━━━━━━━━━━━━━━━━━━━━━━━━━
[ข้อมูลเพิ่มเติม...]
```

---

## 🎯 สรุป

### ✅ ข้อมูลที่รองรับ:
1. **EQNODE** (eqnode.tarita, eqnode.cnx) - แสดงข้อมูลแผ่นดินไหว
2. **Simulator** (earthquake/data) - แสดงข้อมูลทดสอบ
3. **PMAC, TPO** - subscribe แต่ไม่แสดง (log เท่านั้น)

### ✅ การแปลงข้อมูล:
1. **Device ID**: รองรับ `did`, `deviceId`, `device_id`
2. **Magnitude**: คำนวณจาก `pga` หรือใช้ `magnitude` ตรงๆ
3. **Location**: แสดงชื่อหรือพิกัด
4. **Timestamp**: รองรับหลายรูปแบบ

### ✅ การกรองข้อมูล:
1. แสดงเฉพาะ magnitude > 0
2. แจ้งเตือนเฉพาะ magnitude >= 3.0
3. เก็บข้อมูลล่าสุด 20 รายการ

---

## 🔧 ขั้นตอนต่อไป

1. **Rebuild แอพ**:
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

2. **ทดสอบข้อมูลจริง**:
   - เปิดแอพ → แท็บ "MQTT Real-time"
   - ตรวจสอบว่าแสดงข้อมูลจาก EQNODE จริง

3. **ทดสอบ Simulator**:
   ```bash
   cd backend
   node simulate-earthquake.js
   ```

4. **ตรวจสอบผลลัพธ์**:
   - Device ID ถูกต้อง?
   - Magnitude แสดงค่าที่ถูกต้อง?
   - Location แสดงถูกต้อง?
   - Timestamp แสดงถูกต้อง?

---

**สถานะ**: ✅ พร้อมใช้งาน - Rebuild แอพเพื่อทดสอบ
