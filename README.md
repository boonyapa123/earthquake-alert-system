# Earthquake Alert System (eQNode)

แอปพลิเคชันแจ้งเตือนแผ่นดินไหวที่ใช้ Flutter และ MQTT สำหรับการรับข้อมูลแบบ real-time จากอุปกรณ์ IoT

## ✨ คุณสมบัติหลัก

- 🔔 **การแจ้งเตือนแบบ Real-time**: รับการแจ้งเตือนทันทีผ่าน MQTT และ Push Notifications
- 📱 **QR Code Scanner**: สแกน QR Code เพื่อลงทะเบียนอุปกรณ์ใหม่
- 🗺️ **Location Services**: ระบุตำแหน่งอุปกรณ์อัตโนมัติด้วย GPS
- 📊 **Dashboard**: แสดงสถานะอุปกรณ์และข้อมูลล่าสุด
- 📈 **History Logs**: บันทึกเหตุการณ์ทั้งหมดพร้อมการกรองข้อมูล
- ⚙️ **Device Management**: จัดการอุปกรณ์ (ลงทะเบียน/ถอนการติดตั้ง/ย้ายตำแหน่ง)

## 🏗️ สถาปัตยกรรมระบบ

```
┌─────────────────┐    MQTT     ┌──────────────────┐    HTTP/API    ┌─────────────────┐
│   IoT Devices   │ ──────────► │   MQTT Broker    │ ──────────────► │  Backend Server │
│  (Sensors)      │             │ mqtt.uiot.cloud  │                │   (Optional)    │
└─────────────────┘             └──────────────────┘                └─────────────────┘
                                          │
                                          │ MQTT Subscribe
                                          ▼
                                ┌──────────────────┐
                                │  Flutter App     │
                                │  (Mobile/Web)    │
                                └──────────────────┘
```

## 🔧 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น
- Flutter SDK >= 3.3.0
- Dart SDK >= 3.3.0
- Android Studio / VS Code
- Android device หรือ emulator (API level 21+)

### การติดตั้ง

1. **Clone repository**
   ```bash
   git clone <repository-url>
   cd earthquake_app_new
   ```

2. **ติดตั้ง dependencies**
   ```bash
   flutter pub get
   ```

3. **สร้าง network security config directory**
   ```bash
   mkdir -p android/app/src/main/res/xml
   ```

4. **รันแอปพลิเคชัน**
   ```bash
   flutter run
   ```

### การสร้าง APK สำหรับ Production

```bash
# Debug APK
flutter build apk --debug

# Release APK
flutter build apk --release

# APK จะถูกสร้างที่: build/app/outputs/flutter-apk/
```

## 📱 การใช้งาน

### 1. การเข้าสู่ระบบ
- ใช้บัญชีทดสอบ: `user@eqnode.com` / `password123`
- หรือสมัครสมาชิกใหม่

### 2. การลงทะเบียนอุปกรณ์
1. ไปที่หน้า "ตั้งค่าระบบ"
2. กดปุ่ม "สแกน QR Code"
3. สแกน QR Code ของอุปกรณ์ หรือป้อน Device ID ด้วยตนเอง
4. กรอกข้อมูลตำแหน่งและรายละเอียด
5. ระบบจะรับตำแหน่ง GPS อัตโนมัติ

### 3. การดูข้อมูล
- **Dashboard**: แสดงสถานะอุปกรณ์และข้อมูลล่าสุด
- **ประวัติ**: ดูบันทึกเหตุการณ์ทั้งหมดพร้อมการกรอง
- **การแจ้งเตือน**: ตั้งค่าและทดสอบการแจ้งเตือน

## 🔌 การเชื่อมต่อ MQTT

### ข้อมูล MQTT Broker
- **Host**: `mqtt.uiot.cloud`
- **Port**: `1883` (TCP), `8083` (WebSocket)
- **Username**: `ethernet`
- **Password**: `ei8jZz87wx`

### Topics ที่ใช้งาน
- `earthquake/data` - ข้อมูลจากเซ็นเซอร์
- `earthquake/alert` - การแจ้งเตือนฉุกเฉิน
- `earthquake/status` - สถานะอุปกรณ์

### รูปแบบข้อมูล JSON
```json
{
  "deviceId": "EQC-001",
  "magnitude": 4.2,
  "timestamp": "2024-11-02T14:30:00Z",
  "location": "Bangkok, Thailand",
  "type": "earthquake",
  "ownerId": "user@example.com"
}
```

## 🛠️ การพัฒนาและปรับแต่ง

### โครงสร้างโปรเจกต์
```
lib/
├── main.dart                 # Entry point
├── screens/                  # หน้าจอต่างๆ
│   ├── login_screen.dart
│   ├── home_screen.dart
│   ├── qr_scanner_screen.dart
│   └── ...
├── services/                 # Business logic
│   ├── mqtt_manager.dart     # MQTT connection & data
│   ├── api_service.dart      # HTTP API calls
│   ├── notification_service.dart
│   └── user_state.dart       # User & device management
└── ...
```

### การปรับแต่ง MQTT
แก้ไขไฟล์ `lib/services/mqtt_manager.dart`:
```dart
// เปลี่ยน broker information
final String _hostname = 'your-mqtt-broker.com';
final String _username = 'your-username';
final String _password = 'your-password';
```

### การปรับแต่ง Backend API
แก้ไขไฟล์ `lib/services/api_service.dart`:
```dart
// เปลี่ยนเป็น URL ของ Backend จริง
static const String _baseUrl = 'https://your-backend-api.com/api';
static const String _mockMode = 'false'; // เปลี่ยนเป็น false
```

## 🔐 Security Notes

- รหัสผ่าน MQTT ถูก hardcode ในโค้ด (เหมาะสำหรับ demo เท่านั้น)
- ใน production ควรใช้ environment variables หรือ secure storage
- ควรใช้ TLS/SSL สำหรับการเชื่อมต่อ MQTT ใน production

## 🐛 การแก้ไขปัญหา

### ปัญหาการเชื่อมต่อ MQTT
1. ตรวจสอบการเชื่อมต่อ internet
2. ตรวจสอบ firewall settings
3. ลองใช้ MQTT client อื่นทดสอบ (เช่น MQTT Explorer)

### ปัญหา Permissions
1. ตรวจสอบ permissions ใน AndroidManifest.xml
2. อนุญาต permissions ในการตั้งค่าแอป
3. สำหรับ Android 13+ ต้องขออนุญาต notification permission

### ปัญหา QR Scanner
1. ตรวจสอบ camera permission
2. ทดสอบใน device จริง (ไม่ใช่ emulator)
3. ตรวจสอบ QR Code format

## 📄 License

This project is for educational and demonstration purposes.

## 🤝 Contributing

1. Fork the project
2. Create your feature branch
3. Commit your changes
4. Push to the branch
5. Open a Pull Request

---

**หมายเหตุ**: แอปพลิเคชันนี้ใช้ข้อมูลจำลองสำหรับการทดสอบ หากต้องการใช้งานจริงต้องเชื่อมต่อกับ MQTT broker และ backend server ที่เหมาะสม