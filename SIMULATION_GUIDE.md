# 🎮 คู่มือการ Simulate ระบบ eQNode

## 📋 ภาพรวม

ระบบ eQNode สามารถทำงานใน **Simulation Mode** ได้โดยไม่ต้องเชื่อมต่อกับ MQTT cloud จริง เหมาะสำหรับการทดสอบและพัฒนา

## 🎯 โหมดการทำงาน

### 1. **Development Mode (Mock Data)**
- ✅ ใช้ข้อมูลจำลอง (Mock Data)
- ✅ ไม่ต้องเชื่อมต่อ MQTT จริง
- ✅ ไม่ต้องเชื่อมต่อ Backend API
- ✅ สร้างข้อมูลแผ่นดินไหวจำลองทุก 4 วินาที
- ✅ เหมาะสำหรับการพัฒนาและทดสอบ UI

### 2. **Staging Mode (Real Connection)**
- 🔌 เชื่อมต่อ MQTT cloud จริง
- 🔌 เชื่อมต่อ Backend API
- ⚠️ ต้องมี Backend ทำงานอยู่

### 3. **Production Mode**
- 🚀 ใช้งานจริง
- 🔒 มี Security เต็มรูปแบบ

---

## 🚀 วิธีรัน Simulation Mode

### ขั้นตอนที่ 1: ตรวจสอบการตั้งค่า

ไฟล์ `lib/config/app_config.dart` ต้องตั้งค่าเป็น:

```dart
static Environment _environment = Environment.development;

static bool get enableMockData {
  switch (_environment) {
    case Environment.development:
      return true; // ✅ เปิดใช้งาน Mock Data
    // ...
  }
}
```

### ขั้นตอนที่ 2: รันแอพใน Simulator/Emulator

#### สำหรับ iOS Simulator:
```bash
flutter run -d "iPhone 15 Pro"
```

#### สำหรับ Android Emulator:
```bash
flutter run -d emulator-5554
```

#### สำหรับ Chrome (Web):
```bash
flutter run -d chrome
```

### ขั้นตอนที่ 3: ดูรายการ Devices ที่มี

```bash
flutter devices
```

---

## 📱 ฟีเจอร์ที่ทำงานใน Simulation Mode

### ✅ ฟีเจอร์ที่ใช้งานได้

1. **การลงทะเบียนและเข้าสู่ระบบ**
   - ใช้ Mock User: `user@eqnode.com` / `password123`
   - ใช้ Mock Admin: `admin@earthquake.com` / `admin123`

2. **การจำลองข้อมูลแผ่นดินไหว**
   - สร้างข้อมูลทุก 4 วินาที
   - ขนาดแผ่นดินไหว: 1.0 - 7.0 Richter
   - แสดง Notification เมื่อ >= 3.0 Richter

3. **การจัดการอุปกรณ์**
   - เพิ่ม/ลบ/แก้ไขอุปกรณ์
   - ดูสถานะอุปกรณ์
   - สแกน QR Code (ใช้ข้อมูลจำลอง)

4. **Dashboard และสถิติ**
   - แสดงกราฟข้อมูล
   - สถิติ 24 ชั่วโมง / 7 วัน
   - ประวัติการแจ้งเตือน

5. **การแจ้งเตือน**
   - Local Notification
   - แสดงเมื่อมีแผ่นดินไหว >= 3.0

### ⚠️ ฟีเจอร์ที่ไม่ทำงาน (ต้องใช้ Backend จริง)

1. ❌ Firebase Cloud Messaging (FCM)
2. ❌ การซิงค์ข้อมูลกับ Backend
3. ❌ การเชื่อมต่อ MQTT cloud จริง
4. ❌ การแชร์ข้อมูลระหว่างผู้ใช้

---

## 🎨 การทดสอบ UI/UX

### 1. ทดสอบหน้าจอต่างๆ

```bash
# รันแอพ
flutter run

# Hot Reload (กด 'r' ใน terminal)
# Hot Restart (กด 'R' ใน terminal)
```

### 2. ทดสอบ Notification

ในแอพ ไปที่:
- **Settings** → **Test Alert** 
- จะได้รับ notification ทดสอบ

### 3. ทดสอบการสแกน QR Code

ใช้ QR Code ตัวอย่าง:
```json
{
  "deviceId": "EQC-TEST-001",
  "type": "earthquake",
  "name": "Test Sensor 001"
}
```

---

## 🔧 การปรับแต่ง Mock Data

### เปลี่ยนความถี่ในการสร้างข้อมูล

แก้ไขไฟล์ `lib/services/mqtt_manager.dart`:

```dart
_mockTimer = Timer.periodic(
  const Duration(seconds: 4), // เปลี่ยนเป็น 10 วินาที
  (timer) {
    // ...
  }
);
```

### เปลี่ยนขนาดแผ่นดินไหว

```dart
double _generateRandomMagnitude() {
  return 1.0 + Random().nextDouble() * 6.0; // 1.0 - 7.0
}
```

### เพิ่มอุปกรณ์จำลอง

แก้ไขไฟล์ `lib/services/user_state.dart`:

```dart
final List<Map<String, dynamic>> _mockDevices = [
  {
    'id': 'device_001',
    'name': 'Sensor Bangkok',
    'type': 'earthquake',
    'status': 'active',
    // ...
  },
  // เพิ่มอุปกรณ์ใหม่ที่นี่
];
```

---

## 📊 ข้อมูล Mock ที่มีอยู่

### Mock Users
```dart
'user@eqnode.com' / 'password123'
'admin@earthquake.com' / 'admin123'
```

### Mock Devices
- `EQC-MOCK-001` - Bangkok Sensor
- `EQC-MOCK-002` - Chiang Mai Sensor
- `EQC-MOCK-003` - Phuket Sensor

### Mock Earthquake Data
- ขนาด: 1.0 - 7.0 Richter
- ตำแหน่ง: สุ่มจากเมืองใหญ่ในไทย
- ความถี่: ทุก 4 วินาที

---

## 🐛 การ Debug

### เปิด Debug Logging

```dart
// lib/config/app_config.dart
static bool get enableDebugLogging {
  case Environment.development:
    return true; // ✅ เปิดใช้งาน
}
```

### ดู Console Logs

```bash
flutter run --verbose
```

### ดู Widget Inspector

กด `i` ใน terminal หลังจากรันแอพ

---

## 🎯 Checklist การทดสอบ

### หน้าจอหลัก
- [ ] Login Screen
- [ ] Register Screen
- [ ] Home Screen (Dashboard)
- [ ] Device List
- [ ] Device Details
- [ ] Settings

### ฟีเจอร์
- [ ] Login/Logout
- [ ] ดูข้อมูลแผ่นดินไหวแบบ Real-time
- [ ] รับ Notification
- [ ] เพิ่ม/ลบอุปกรณ์
- [ ] ดูกราฟและสถิติ
- [ ] เปลี่ยนธีม (Light/Dark)

### Performance
- [ ] แอพเปิดเร็ว (< 3 วินาที)
- [ ] Smooth scrolling
- [ ] ไม่มี memory leak
- [ ] Battery usage ปกติ

---

## 🚨 Troubleshooting

### ปัญหา: ไม่มีข้อมูลแสดง

**วิธีแก้:**
1. ตรวจสอบว่า `enableMockData = true`
2. ดู console logs
3. Restart แอพ (กด 'R')

### ปัญหา: Notification ไม่แสดง

**วิธีแก้:**
1. ตรวจสอบ Permission
2. ไปที่ Settings → Test Alert
3. ดู console logs

### ปัญหา: แอพช้า

**วิธีแก้:**
1. รันใน Release mode: `flutter run --release`
2. ปิด Debug logging
3. ลด frequency ของ mock data

---

## 📝 หมายเหตุ

- Simulation Mode เหมาะสำหรับการพัฒนา UI/UX
- ข้อมูลจะไม่ถูกบันทึกถาวร (เก็บใน memory เท่านั้น)
- เมื่อพร้อม deploy ต้องเปลี่ยนเป็น Production Mode

---

## 🔄 การเปลี่ยนโหมด

### เปลี่ยนเป็น Staging Mode

```dart
// lib/main.dart
void main() {
  AppConfig.setEnvironment(Environment.staging);
  runApp(const MyApp());
}
```

### เปลี่ยนเป็น Production Mode

```dart
// lib/main.dart
void main() {
  AppConfig.setEnvironment(Environment.production);
  runApp(const MyApp());
}
```

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:
- ดู logs ใน console
- ตรวจสอบ `AppConfig.printConfig()`
- อ่าน error messages

---

**สนุกกับการพัฒนา! 🚀**
