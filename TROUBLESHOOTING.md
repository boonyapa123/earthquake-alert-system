# 🔧 แก้ปัญหา APK ติดตั้งได้แต่เปิดไม่ได้

## ❌ ปัญหาที่พบ
```
Error waiting for a debug connection: The log reader stopped unexpectedly
Error launching application on sdk gphone64 arm64
```

---

## ✅ วิธีแก้ไข

### **วิธีที่ 1: Build APK แบบ Release (แนะนำ)**

APK debug มีปัญหากับ emulator บางตัว ให้ build แบบ release แทน:

```bash
# Build release APK
flutter build apk --release

# ไฟล์จะอยู่ที่
# build/app/outputs/flutter-apk/app-release.apk
```

**ติดตั้งบนมือถือ:**
1. Copy `app-release.apk` ไปยังมือถือ
2. เปิดไฟล์และติดตั้ง
3. อนุญาต "Install from unknown sources"
4. เปิดแอพได้เลย

---

### **วิธีที่ 2: ใช้ Physical Device แทน Emulator**

```bash
# เชื่อมมือถือกับ USB
# เปิด USB Debugging บนมือถือ

# เช็คว่าเจอมือถือหรือไม่
flutter devices

# รันบนมือถือ
flutter run
```

---

### **วิธีที่ 3: Restart Emulator**

```bash
# หยุด Flutter process
# กด Ctrl+C

# ปิด emulator
# เปิด emulator ใหม่

# รันใหม่
flutter run -d emulator-5554
```

---

### **วิธีที่ 4: ใช้ iOS Simulator (ถ้ามี Mac)**

```bash
flutter run -d "iPhone 15 Pro"
```

---

## 📱 วิธีติดตั้ง APK บนมือถือจริง

### ขั้นตอนที่ 1: Build APK

```bash
# Build release APK
flutter build apk --release

# หรือ build debug APK
flutter build apk --debug
```

### ขั้นตอนที่ 2: Copy APK ไปมือถือ

**ตำแหน่งไฟล์:**
- Release: `build/app/outputs/flutter-apk/app-release.apk`
- Debug: `build/app/outputs/flutter-apk/app-debug.apk`

**วิธี Copy:**
1. **ผ่าน USB:** Copy ไฟล์ไปยัง Downloads folder
2. **ผ่าน AirDrop:** (iOS/Mac)
3. **ผ่าน Google Drive:** Upload แล้ว download บนมือถือ
4. **ผ่าน Email:** ส่งไฟล์ให้ตัวเอง

### ขั้นตอนที่ 3: ติดตั้งบนมือถือ

**Android:**
1. เปิดไฟล์ APK
2. อนุญาต "Install from unknown sources"
3. กด Install
4. เปิดแอพ

**หมายเหตุ:**
- ต้องเปิด "Unknown sources" ใน Settings → Security
- บางเครื่องต้องอนุญาตต่อแอพ (เช่น Chrome, Files)

---

## 🔍 เช็คว่า Backend ทำงานหรือไม่

### ถ้าใช้ WiFi เดียวกัน:

```bash
# เช็คว่า Backend ทำงานหรือไม่
curl http://10.134.94.222:3000/api/v1/health

# ถ้าได้ response แสดงว่า Backend ทำงาน
```

### ถ้าใช้ 4G/5G:

Backend จะเชื่อมต่อไม่ได้ ต้อง deploy ไปบน cloud ก่อน (ดู DEPLOYMENT_OPTIONS.md)

---

## 🎯 แนะนำ: Build Release APK

```bash
# 1. Build release APK
flutter build apk --release

# 2. ไฟล์จะอยู่ที่
ls -lh build/app/outputs/flutter-apk/app-release.apk

# 3. Copy ไปมือถือ
# - ผ่าน USB
# - ผ่าน Google Drive
# - ผ่าน Email

# 4. ติดตั้งบนมือถือ
# - เปิดไฟล์ APK
# - อนุญาต install from unknown sources
# - กด Install
```

---

## ⚙️ ตรวจสอบการตั้งค่า

### เช็ค Backend URL:

```dart
// lib/config/app_config.dart
static String get baseUrl {
  switch (_environment) {
    case Environment.development:
      return 'http://10.134.94.222:3000/api/v1'; // ใช้ได้เฉพาะ WiFi เดียวกัน
    case Environment.production:
      return 'https://your-app.railway.app/api/v1'; // ใช้ได้ทุกที่
  }
}
```

### เช็ค Environment:

```dart
// lib/main.dart
void main() {
  // Development (WiFi เดียวกัน)
  AppConfig.setEnvironment(Environment.development);
  
  // Production (ใช้ได้ทุกที่)
  // AppConfig.setEnvironment(Environment.production);
  
  runApp(const MyApp());
}
```

---

## 📋 Checklist

- [ ] Build release APK: `flutter build apk --release`
- [ ] Copy APK ไปมือถือ
- [ ] อนุญาต "Install from unknown sources"
- [ ] ติดตั้ง APK
- [ ] เช็คว่ามือถือและ Mac อยู่ใน WiFi เดียวกัน
- [ ] เช็คว่า Backend ทำงานอยู่: `npm run dev`
- [ ] เปิดแอพและทดสอบ

---

## 🆘 ยังไม่ได้?

### ปัญหา: แอพเปิดแล้วไม่มีข้อมูล

**สาเหตุ:**
- Backend ไม่ทำงาน
- มือถือไม่ได้อยู่ใน WiFi เดียวกัน
- URL ไม่ถูกต้อง

**วิธีแก้:**
1. เช็ค Backend: `curl http://10.134.94.222:3000/api/v1/health`
2. เช็ค WiFi: มือถือต้องเชื่อมต่อ WiFi เดียวกับ Mac
3. ดู logs: เปิด Android Studio → Logcat

### ปัญหา: ติดตั้งไม่ได้

**สาเหตุ:**
- ไม่ได้อนุญาต "Unknown sources"

**วิธีแก้:**
1. Settings → Security → Unknown sources → เปิด
2. หรือ Settings → Apps → Chrome → Install unknown apps → อนุญาต

---

## 🎉 สรุป

**วิธีที่ง่ายที่สุด:**

```bash
# 1. Build APK
flutter build apk --release

# 2. Copy ไปมือถือ (ผ่าน Google Drive หรือ USB)

# 3. ติดตั้งบนมือถือ

# 4. เปิด Backend บน Mac
cd backend && npm run dev

# 5. เชื่อมมือถือกับ WiFi เดียวกับ Mac

# 6. เปิดแอพและใช้งาน
```

**พร้อมใช้งาน!** 🚀
